[package]
name = "platform_host"
version = "0.1.0"
edition = "2021"

[dependencies]
error-model = { path = "../../../shared/error-model" }
js-sys = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror.workspace = true

[dev-dependencies]
futures = "0.3"
