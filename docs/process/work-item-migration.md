# Work Item Migration

The repository previously tracked work through committed `work-items/` artifacts and a dedicated validator. That process is archived. GitHub issues, pull requests, milestones, and the organization Project are now the active system of record. Repo-linked execution artifacts under `plans/` are an active companion surface for long or high-risk work, not a reactivation of `work-items/`.

## Active Item Migration

No open `work-items/` remained at the time of cutover on 2026-03-05. No GitHub issues had to be created to preserve active execution state.

## Historical Mapping

| Historical work item | Final state | GitHub issue |
| --- | --- | --- |
| `short-origin-recursive-chatgpt-method` | archived historical record | none; completed before cutover |
| `short-origin-process-contracts` | archived historical record | none; completed before cutover |
| `short-origin-prompt-procedures` | archived historical record | none; completed before cutover |
| `short-origin-validator-enforcement` | archived historical record | none; completed before cutover |
| `short-origin-wasmcloud-ui-shell` | archived historical record | none; completed before cutover |

The `work-items/` tree remains in git as read-only history and should not receive new entries.
